# llm-systems-alarm-engine dependencies
# Last audit: 2026-05-15

# Web framework — uvicorn[standard] pulls in websockets + httptools.
fastapi>=0.115.0
fastapi[standard]>=0.136.1
uvicorn[standard]>=0.34.0

# InfluxDB v2 client
influxdb-client>=1.50.0

# OpenTelemetry protobuf bindings (OTLP/HTTP receiver in backend/receivers/)
opentelemetry-proto>=1.25.0

# HTTP clients
httpx>=0.27.0
requests>=2.33.0

# Data validation + settings loading from env vars
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Statistical analysis for richer anomaly detection (Z-score, percentile
# distributions, time-series correlation). The current anomaly_detector
# uses only stdlib math; these are kept available for incremental
# enhancement without a future requirements bump.
# Wheels require x86-64-v2+; the manager VM now runs on a Ryzen V1500B
# which supports x86-64-v3, so the no-baseline workarounds aren't needed.
numpy>=2.1.0
scipy>=1.14.0

# AES-GCM + scrypt for the admin backup/restore archive format
# (backend/_archive.py).
cryptography>=42.0.0
